For ease, we dont implement any kind of type checking, but we deal with type as a convention. For example $cv: \integer$, means that the allowed processes to be initialized with $cv$ are: $Zero, One, Two, Three$.